<?php
 $where = __DIR__
 $serverdo = 'cd' $where '|git pull'
 $serverdid = shell_exec($serverdo);
print $serverdid;

/***
 * A webhook for auto clone $ pull
 * Not full code, need another code to make it run.
 * It's not running version at server now.
 */

?>